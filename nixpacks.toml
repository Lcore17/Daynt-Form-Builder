[phases.setup]
nixPkgs = ['nodejs_20']

[phases.install]
cmds = ['cd apps/api && npm install']

[phases.build]
cmds = [
  'cd apps/api',
  'npx prisma generate',
  'npm run build'
]

[start]
cmd = 'cd apps/api && npm run start:prod'
